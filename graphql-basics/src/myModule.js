const name = "Abnish"
const location = "Patna"
const message = "Hi, Its time to code";

const greetings =(location)=>{
    return `welcome to ${location}`
}

export { name, location, message as default, greetings }